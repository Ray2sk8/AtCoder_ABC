#include <bits/stdc++.h>

using namespace std;
using ll = long long;

ll N;
vector<int>

    int main() {

    int Q;
    cin >> Q;
    for (int i = 0; i < Q; i++) {
        int q;
        int a, b;
        cin >> q;
        if (q == 1) {
            cin >> a >> b;
        } else if (q == 2) {
            askBox();
        } else if (q == 3) {
            askCard();
        }
    }

    

    return 0;
}