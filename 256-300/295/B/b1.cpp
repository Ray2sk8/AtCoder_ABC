#include <bits/stdc++.h>

using namespace std;
using ll = long long;

ll R, C;
vector<string> town(20);

int main(){
    cin >> R >> C;

    for (int i = 0; i < R; i++) {
        cin >> town.at(i);
    }

    return 0;
}