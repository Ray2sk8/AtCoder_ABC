#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {

    // int N, M;
    // cin >> N >> M;
    // vector<string> str(N + 1, 0);

    // vector<vector<int>> dp(N + 1, vector<int>(M + 1, 0));
    // dp[0][0] = false;
    // vector<vector<int>> path;
    // // dp
    // for (int i = 1; i <= N; i++) {
    //     for(int j = 0; j < M; j++) {
    //         if(str[j] == "o"){
    //             dp[i][j] +=1;
    //         }
    //     }
    // }

    int i = 1 << 0;
    cout << i << endl;
    i = 1 << 1;
    cout << i << endl;

    return 0;
}